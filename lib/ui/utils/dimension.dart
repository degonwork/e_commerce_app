import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  // pageView
  static double pageView = screenHeight / 2.51;
  static double pageViewContainer = screenHeight / 3.65;
  static double pageViewTextContainer = screenHeight / 6.697;

  // height
  static double height10 = screenHeight / 80.367;
  static double height15 = screenHeight / 53.578;
  static double height20 = screenHeight / 40.18;
  static double height30 = screenHeight / 26.787;
  static double height45 = screenHeight / 17.86;

  // width
  static double width10 = screenHeight / 80.367;
  static double width15 = screenHeight / 53.578;
  static double width20 = screenHeight / 40.18;
  static double width30 = screenHeight / 26.787;
  static double width45 = screenHeight / 17.86;

  // font
  static double font16 = screenHeight / 50.23;
  static double font20 = screenHeight / 40.18;
  static double font26 = screenHeight / 30.91;

  // radius
  static double radius15 = screenHeight / 53.578;
  static double radius20 = screenHeight / 40.18;
  static double radius30 = screenHeight / 26.787;

  // icon size
  static double iconSize24 = screenHeight / 33.48;
  static double iconSize16 = screenHeight / 50.23;

  // Listview size
  static double listViewImgSize = screenWidth / 3.27;
  static double listViewTextContSize = screenWidth / 3.93;

  // popular food
  static double popularFoodImgSize = screenHeight / 2.3;

  // navigator
  static double bottomHeightBar = screenHeight / 6.697;

  // SplashImg
  static double splashImg = screenHeight / 3.21;
}
